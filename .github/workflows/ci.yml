name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  POETRY_VERSION: "1.8.3"

jobs:
  test:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install Poetry
        run: python -m pip install "poetry==$POETRY_VERSION"
      - name: Install locked dependencies
        run: poetry install --with dev --sync
      - name: Validate metadata and lock
        run: poetry check
      - name: Run tests with production coverage gate
        run: poetry run pytest src/html2md/tests tests/config --cov=html2md --cov-report=term-missing:skip-covered

  package:
    name: Build and wheel smoke test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.11"
      - name: Install Poetry
        run: python -m pip install "poetry==$POETRY_VERSION"
      - name: Install locked development tools
        run: poetry install --with dev --sync
      - name: Build distributions
        run: poetry build
      - name: Validate distribution metadata and rendered README
        run: poetry run twine check dist/*
      - name: Verify wheel contents
        run: |
          python - <<'PY'
          from pathlib import Path
          from zipfile import ZipFile

          wheel = next(Path("dist").glob("*.whl"))
          with ZipFile(wheel) as archive:
              names = archive.namelist()
          assert not any(name.startswith("html2md/tests/") for name in names)
          assert "html2md/html2md" not in names
          assert "html2md/__main__.py" in names
          PY
      - name: Run extension static regressions
        run: |
          for file in extension/*.js extension/tests/*.js; do node --check "$file"; done
          node --test extension/tests/*.test.js
      - name: Install and exercise the wheel
        run: |
          poetry run python scripts/release_smoke.py dist/*.whl \
            --expected-version "$(poetry version --short)"

  quality:
    name: Static quality gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.11"
      - name: Install Poetry
        run: python -m pip install "poetry==$POETRY_VERSION"
      - name: Install locked dependencies
        run: poetry install --with dev --sync
      - name: Ruff
        run: poetry run ruff check src/html2md tests/config
      - name: Black
        run: poetry run black --check src/html2md tests/config
      - name: Mypy
        run: |
          poetry run mypy src/html2md tests/config
          poetry run mypy --check-untyped-defs \
            --exclude 'src/html2md/tests/' src/html2md
      - name: Bandit
        run: poetry run bandit -r src/html2md -x src/html2md/tests -ll
      - name: Audit locked development environment
        run: poetry run pip-audit

  portable:
    name: Portable package (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.11"
      - name: Install Poetry
        run: python -m pip install "poetry==$POETRY_VERSION"
      - name: Install locked dependencies
        run: poetry install --with dev --sync
      - name: Validate metadata and platform contracts
        run: |
          poetry check
          poetry run pytest -q src/html2md/tests tests/config
      - name: Build and smoke-test the installed wheel
        run: |
          poetry build
          python -c "from pathlib import Path; import subprocess, sys; subprocess.check_call([sys.executable, '-m', 'pip', 'install', '--force-reinstall', '--no-deps', str(next(Path('dist').glob('*.whl')))])"
          python -I -m html2md --help

  browser-render:
    name: Chromium runtime checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.11"
      - name: Install Poetry
        run: python -m pip install "poetry==$POETRY_VERSION"
      - name: Install render extra and Chromium
        run: |
          poetry install --with dev --extras render --sync
          poetry run playwright install --with-deps chromium
      - name: Audit render dependencies
        run: |
          poetry export --only main --extras render --without-hashes \
            --format requirements.txt --output "$RUNNER_TEMP/render-requirements.txt"
          poetry run pip-audit --requirement "$RUNNER_TEMP/render-requirements.txt"
      - name: Exercise static and rendered fixtures
        env:
          HTML2MD_RUN_RENDER_E2E: "1"
        run: |
          poetry run pytest -q src/html2md/tests/test_browser_renderer_e2e.py
          export CHROME_BIN="$(poetry run python -c 'from playwright.sync_api import sync_playwright; p = sync_playwright().start(); print(p.chromium.executable_path); p.stop()')"
          node extension/tests/chromium-smoke.js
